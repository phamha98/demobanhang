export default function choiseColor(){
    
const choiseColor = (a) => {
    if (a === "Red") return "red";
    if (a === "Multicoloured") return "red";
    if (a === "Brown") return "brown";
    if (a === "White") return "white";
    if (a === "Anthracite grey") return "#343E40";

    return "#000";
  };
  const backGround=(a)=>{
    if (a === "White") return "gray";
    if (a === "Multicoloured") return "yellow";
    return 'white';
  }
    }